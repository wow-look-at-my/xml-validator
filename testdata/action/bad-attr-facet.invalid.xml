<?xml version="1.1" encoding="UTF-8"?>
<!-- @id is an ATTRIBUTE, and the facet on it is the one a regression drops
     first: element text keeps working, so the schema still looks enforced. -->
<cfg>
	<item id="ALPHA1"/>
</cfg>
